language: r

sudo: required

warnings_are_errors: true

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change

apt_packages:
 - r-cran-rjava

r_packages:
 - ggplot2
 - rJava
 - ReporteRsjars

os:
 - linux
 - osx
